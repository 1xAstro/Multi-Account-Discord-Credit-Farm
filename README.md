This farm works by adding however many discord account tokens you want in each client field. 
The farm then automatically sends the #daily message on all registered servers on those accounts to generate the Probot digital credits.
**Make sure to run this on something like a docker instance or a tmux session to make sure it runs 24/7.**
